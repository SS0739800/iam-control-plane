# Deliberately short. This is a downstream system, not a second copy of the
# platform's stack — no ORM, no migrations, no SAML library. One table in SQLite
# through the standard library.
fastapi==0.120.4
uvicorn[standard]==0.40.0
