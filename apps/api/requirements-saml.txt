# SAML dependencies — LINUX / DOCKER ONLY. Do not install into a Windows venv.
#
# xmlsec has no usable Windows wheels, and mixing a prebuilt lxml wheel with a
# source-built xmlsec segfaults at runtime rather than failing at install time.
# The Dockerfile therefore builds BOTH from source with --no-binary. See
# docs/adr/0004-build-xmlsec-from-source.md.
#
# Not needed until P2. Kept here so the recipe is pinned from day one.

lxml==5.3.0
xmlsec==1.3.14
python3-saml==1.16.0
